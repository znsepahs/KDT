#--------------------------------------------------------------------------
# 모듈 = 파이썬 파일(py) 1개
# - 구성 : 변수, 함수, 클래스가 존재
# -        반드시 모두 다 있지는 않음!
# - 종류 : 내장모듈 / 사용자 정의 모듈 / 써드 파티션 모듈(설치 필수)
#--------------------------------------------------------------------------
import myfunc as mf

print(mf.add(10,4))